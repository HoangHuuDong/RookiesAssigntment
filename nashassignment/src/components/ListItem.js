import React from "react";

const ListItem = props =>{
    // const list = props.listProps
    
    const listStyle = {
        background: 'white',
        padding: '10px',
    }

    return <p style={listStyle}>Hello</p>
}

export default ListItem;